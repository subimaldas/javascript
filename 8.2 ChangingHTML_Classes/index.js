first.className = "text-black red"
first.classList.add("red")
first.classList.remove("red")
first.classList.contains("red")  //check  the class or not return(true & false)
first.classList.toggle("red")    //if exist skip if not add class
